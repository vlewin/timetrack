.row
  .col-sm-6.col-md-6.left
    %a.day.today{href: "/timetracks?date=#{Date.today.strftime("%Y-%m-%d")}"}
      %i.fa.fa-clock-o
      %b TimeTrack
  .col-sm-6.col-md-6.right
    - if user_signed_in?
      %i.fa.fa-clock-o
      %b= Time.zone

      %i.fa.fa-user
      %b= current_user.username
      = link_to('(logout)', destroy_user_session_path, :method => :delete)

.row
  .col-md-12.center
    - if user_signed_in?
      -if notice
        %span.alert.alert-success
          %i.fa.fa-check-circle-o
          = notice
      - if alert
        %span.alert
          %i.fa.fa-warning
          = alert
      -unless notice || alert
        &nbsp;

#timetrack
  = render "timetrack"

